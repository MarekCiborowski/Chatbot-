@model AdminPanel.ViewModels.AdminViewModels.QuestionViewModels.DeleteQuestionVM

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>DeleteQuestionVM</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color">
            <input type="submit" value="Delete" class="btn btn-danger" /> 
            <input type="button" class=" btn btn-outline-dark" value="Index" onclick="location.href='@Url.Action("Index", "Question")'" />
        </div>
    }
</div>
